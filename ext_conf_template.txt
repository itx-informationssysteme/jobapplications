# "Google for Jobs Configuration"
####################################
# cat=general; type=int; label=LLL:EXT:jobapplications/Resources/Private/Language/locallang_backend.xlf:conf.customFileSizeLimit
customFileSizeLimit =

# cat=general; type=string; label=Allowed application upload file types:As comma separated list e.g. .pdf,.docx
allowedFileTypes = .pdf

# cat=general; type=options[uniqueInSite=uniqueInSite,unique=unique]; label=Slug behavior:"uniqueInSite": The same slug can be used for postings in different sites. Use this setting only if no posting records are shared between sites. "unique" means that same posting title will lead to different slug names.
slugBehaviour = uniqueInSite

# "Google Indexing API Configuration"
####################################
# cat=google; type=boolean; label=Enable Google Indexing API:Enables sending crawl/removal requests to Google when a job posting is created, updated, hidden, or deleted.
indexing_api = 0

# cat=google; type=string; label=Path to Google service account key:Absolute or EXT: path to the .json service account key file (e.g. EXT:jobapplications/../../google-indexing-key.json). Keep this file outside the public webroot.
key_path =

# cat=google; type=boolean; label=Enable in development context:If disabled, requests are skipped entirely while TYPO3 is in Development context, even if the API is enabled above. Recommended off, to avoid accidentally indexing/deindexing production URLs from local or staging environments.
indexing_api_dev = 0

# cat=google; type=boolean; label=Enable debug flash messages:Shows a backend flash message for every indexing request attempt (success or failure), including the generated URL and any error response from Google.
indexing_api_debug = 0
